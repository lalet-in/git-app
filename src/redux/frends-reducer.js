
let initialState = {
    frends: [
        {id: 1, frends: 'Dimych'},
        {id: 2, frends: 'Pimych'},
        {id: 3, frends: 'Zimych'}
    ]
}

const frendsReducer = (state = initialState, action) => {

    return state;
}

export default frendsReducer;